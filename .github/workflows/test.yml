name: Basic Test

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest  # Используем Linux вместо MacOS

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print hello
        run: |
          echo "🎉 Hello from GitHub Actions!"
          echo "📁 Project structure:"
          ls -la
          echo "✅ If you see this - GITHUB ACTIONS WORKS!"