name: Build iOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Разрешает ручной запуск

jobs:
  build-ios:
    runs-on: macos-latest  # ⚠️ ТОЛЬКО MacOS!

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🍎 Setup Xcode
        uses: apple-actions/setup-xcode@v1
        with:
          xcode-version: '15.2'

      - name: 🔨 Build KMP for iOS
        run: |
          echo "Building shared module for iOS..."
          ./gradlew :shared:assembleIosX64
          ./gradlew :shared:assembleIosArm64
          echo "✅ iOS frameworks built!"

      - name: 📦 Upload iOS Frameworks
        uses: actions/upload-artifact@v4
        with:
          name: iOS-Frameworks
          path: shared/build/bin/
          retention-days: 7